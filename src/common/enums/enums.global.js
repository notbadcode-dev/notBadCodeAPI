export const AppIds = Object.freeze({
    codes: 1,
});

export const MessageType = Object.freeze({
    default: 0,
    permission: 5,
    info: 10,
    success: 15,
    warning: 20,
    error: 25,
    critical: 90
});
